# github-sync-manager
A safe, conservative GitHub synchronization tool that clones and updates all repositories and gists for a user, with built‑in rate‑limit handling, retry logic, and protection against data loss. By design, no changes are pushed to remote.
